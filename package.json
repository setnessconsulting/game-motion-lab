{
  "name": "game-motion-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Motion Lab - a mission-based middle-school forces-and-motion investigation game (NGSS MS-PS2-2).",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests scripts vite.config.ts vitest.config.ts playwright.config.ts playwright.phaser.config.ts playwright.accessibility.config.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "contracts": "node scripts/verify-contracts.mjs",
    "foundation": "node scripts/verify-foundation.mjs",
    "test:e2e": "npm run build && npm run test:e2e:run",
    "test:e2e:run": "playwright test",
    "test:phaser-render": "playwright test -c playwright.phaser.config.ts",
    "test:a11y": "playwright test -c playwright.accessibility.config.ts",
    "perf:baseline": "node scripts/perf-baseline.mjs",
    "perf:check": "node scripts/check-bundle-budget.mjs",
    "verify": "npm run contracts && npm run foundation && npm run typecheck && npm run lint && npm run test && npm run build && npm run perf:check && npm run test:e2e:run && npm run test:phaser-render && npm run test:a11y"
  },
  "dependencies": {
    "phaser": "4.2.1",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.1.1",
    "eslint": "^10.11.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.12.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.0",
    "vite": "^8.3.0",
    "vitest": "^4.1.11"
  }
}
